// Firebase is temporarily disabled for this session.
// Exports are nulled to allow the app to run in "Demo Mode" without external dependencies.

export const app = null;
export const auth = null;
export const db = null;
export const googleProvider = null;

export default app;